<div>
  <%= live_render(@socket, HerosWeb.HeaderLive, id: "header", session: @session) %>

  <h1><%= @game.name %></h1>

  <label>Public : <input type="checkbox" <%= if @game.is_public, do: "checked" %> disabled></label>

  <div><%= length(@game.players) %> / <%= @game.max_players %> joueurs</div>

  <table>
    <%= for player <- @game.players do %>
      <tr>
        <td><%= if player.is_admin, do: "admin" %></td>
        <td><%= player.name %></td>
      </tr>
    <% end %>
  </table>

  <center>
    <button phx-click="leave">quitter</button>
  </center>
</div>
